--- Python-2.7.2.orig/Lib/ctypes/util.py	2011-06-11 16:46:24.000000000 +0100
+++ Python-2.7.2/Lib/ctypes/util.py	2015-03-25 13:42:46.981611712 +0000
@@ -3,6 +3,24 @@
 ######################################################################
 import sys, os
 
+if os.name == "posix" and sys.platform == "linux3":
+    def find_library(name):
+        """ hack to find librarys for kivy and android """
+        print "test import files"
+        app_root = os.path.abspath('./').split(os.path.sep)[0:3]
+        print app_root
+        lib_search = os.path.sep.join(app_root) + os.path.sep + 'files'
+        print lib_search
+        print os.listdir(lib_search)
+        find_library 
+        for filename in os.listdir(lib_search):
+            if filename.endswith('.so') and name in filename:
+                print 'found '
+                print lib_search + os.path.sep + filename
+                return lib_search + os.path.sep + filename
+        print 'not found'
+        return None
+
 # find_library(name) returns the pathname of a library, or None.
 if os.name == "nt":
 
@@ -84,7 +102,7 @@
                 continue
         return None
 
-elif os.name == "posix":
+elif os.name == "posix-old":
     # Andreas Degert's find functions, using gcc, /sbin/ldconfig, objdump
     import re, tempfile, errno
 
